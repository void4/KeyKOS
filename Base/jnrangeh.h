int checkcdainrange(
   unsigned char *cda,          /* The CDA to check */
   struct Key *rk);
LLI offset;  /* value returned from checkcdainrange */
unsigned char *validaterelativecda(
   struct Key *rk);
unsigned char *validaterelativecda6(
   struct Key *rk);

