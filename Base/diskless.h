#if 0 /* we set this macro via the makefile */
#define diskless_kernel 0
/* With this switch set to 1 one can use a primordial range key
   yielding CDAs from 4096 to 8000.  Frames are alloted on demand.
   Page frames are not cleaned nor stolen. */
#endif
