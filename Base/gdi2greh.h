/* Routines in GDIRECTC called only by GRESTARC */
 
extern int idibde(PAGE p, uint64 checkpointtod); /* 1 if dir page OK */
 
extern void idiirap(void);
 
extern uchar *idinap(void);   /* Returns pointer to CDA or NULL */
 
