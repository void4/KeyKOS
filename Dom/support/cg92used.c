/* This file contains a definition for a variable referenced by
 * compiler emitted code. The symbol is normally resolved by an
 * object passed to ld by the compiler. We don't link using the
 * standard mechanism so we define __cg92_used here.
 */
int __cg92_used;
